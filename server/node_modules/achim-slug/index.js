module.exports = function (str) {
  var tobeslug = str.toLowerCase().split(' ')
  var newDate = new Date()
  var getTime = `${newDate.getHours()}-${newDate.getMinutes()}-${newDate.getSeconds()}`
  tobeslug.unshift(getTime)
  return tobeslug.join('-')
}
